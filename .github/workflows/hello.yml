name: First Pipeline
on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:
jobs:
  great:
    runs-on: ubuntu-latest
    steps:
      - name: Before checkout
        run: ls -la
      - name: Checkout code
        uses: actions/checkout@v4
      - name: After checkout
        run: ls -la
        
